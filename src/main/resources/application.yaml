server:
  port: ${PORT:8081}

spring:
  profiles:
    active: prod

  datasource:
    url: jdbc:postgresql://db.ingjkgihejitvnhxsdxd.supabase.co:5432/postgres?sslmode=require
    username: postgres
    password: 'gamelogson@@'
    driver-class-name: org.postgresql.Driver

  sql:
    init:
      mode: never
      schema-locations: classpath:schema-postgresql.sql
      continue-on-error: true

logging:
  level:
    org.springframework: DEBUG
